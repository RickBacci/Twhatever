.row
  .col-md-6.col-md-offset-3.text-center
    %p hello
    = current_user
    -if current_user
      %h1= "Hello, #{current_user.name.upcase}!"
      %h1 Twitter Clone
      %p @rickbacci_175
    -else
      =link_to "Login", login_path
